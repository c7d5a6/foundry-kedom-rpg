{
  "$comment": "Copy to foundry-config.json (gitignored). dataPath is the Foundry user-data root (contains Data/), relative to the repo root.",
  "dataPath": "../foundrydata",
  "foundryUrl": "http://localhost:30000",
  "gamemasterUser": "Gamemaster"
}
